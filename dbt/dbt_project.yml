name: fincore
version: "1.0.0"
config-version: 2

profile: fincore

model-paths:
  - models

test-paths:
  - tests

seed-paths:
  - seeds

clean-targets:
  - target
  - dbt_packages

models:
  fincore:
    staging:
      +schema: staging
      +materialized: view

    marts:
      +schema: marts
      +materialized: table
